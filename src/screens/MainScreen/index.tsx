import { StyleSheet, Text, View } from 'react-native'
import { app } from '../../theme'

const MainScreen = () => {
  return (
    <View>
      <Text style={app.paragraph}>
        Here's come calculator
      </Text>
    </View>
  )
}

const styles = StyleSheet.create({
  
})

export default MainScreen
